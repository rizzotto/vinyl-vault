import { Inter, Salsa } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const playfair = Salsa({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
